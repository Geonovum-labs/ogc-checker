import { Spec } from './types';

const specs: Spec[] = [
  {
    name: 'JSON-FG',
    slug: 'json-fg',
  },
  {
    name: 'OGC API Features',
    slug: 'ogcapi-features',
  },
];

export default specs;
